version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ng build
  artifacts:
    baseDirectory: dist/tallereventloop/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
