version: 1
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/tallereventloop
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
